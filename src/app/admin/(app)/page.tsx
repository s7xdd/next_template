"use client";

import { SectionTitle } from "@/components/custom-styles/styles";
import React from "react";

const Dashboard = () => {
  return (
    <div className="p-6">
      <SectionTitle>Dashboard</SectionTitle>
    </div>
  );
};

export default Dashboard;
